# Java-Hybrid-User-Auth-System
A two-part Java application for user management, featuring a JavaFX GUI login window and a console-based management environment. User data (credentials) is persistently stored in a local file, simulating a flat-file database backend.
